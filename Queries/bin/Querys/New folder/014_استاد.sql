/*	014	Ostad	*/
IF COL_LENGTH('TABLENAME','Ostad') IS NULL BEGIN ALTER TABLE [TABLENAME] ADD Ostad NVARCHAR(100) END
GO
IF COL_LENGTH('TABLENAME','Ostad') IS NOT NULL BEGIN UPDATE [TABLENAME] SET Ostad=NULL END

UPDATE a SET a.Ostad='استاد' FROM [TABLENAME] a
JOIN (SELECT COUNT(*)cnt,TeacherID,StudentID FROM AmarPartDB.dbo.TBL_Teacher GROUP BY TeacherID,StudentID) b
ON b.StudentID = a.StudentID
